# 更新 Vercel 部署指南

## ✅ 当前状态

所有更改已成功推送到 GitHub：
- ✅ vercel.json 配置已修复（使用 rewrites 替代 routes）
- ✅ holland_test_preview.html 已更新（120题完整版）
- ✅ 所有辅助脚本和文档已提交

## 🚀 更新 Vercel 部署

### 方法 1：自动部署（推荐）

如果 Vercel 已连接 GitHub 仓库，推送后会自动触发部署：

1. **检查部署状态**
   - 访问 Vercel 控制台：https://vercel.com/dashboard
   - 找到项目：`holland-test`
   - 查看最新的部署状态

2. **等待部署完成**
   - 通常需要 1-2 分钟
   - 状态会显示为 "Building" → "Ready"

3. **验证部署**
   - 访问你的域名：`https://holland-test.snowshadow.com.cn/`
   - 应该能看到更新后的测试页面

### 方法 2：手动触发部署

如果自动部署没有触发，可以手动重新部署：

1. **在 Vercel 控制台**
   - 访问：https://vercel.com/dashboard
   - 找到项目 `holland-test`
   - 点击 "Deployments" 标签
   - 找到最新的部署，点击 "..." 菜单
   - 选择 "Redeploy"

2. **使用 Vercel CLI（如果已安装）**
   ```bash
   vercel --prod
   ```

### 方法 3：推送新提交触发部署

如果自动部署没有工作，可以创建一个空提交：

```bash
git commit --allow-empty -m "Trigger Vercel deployment"
git push origin main
```

## 📋 部署配置检查清单

### vercel.json 配置

确保 `vercel.json` 使用正确的配置：

```json
{
  "rewrites": [
    {
      "source": "/",
      "destination": "/holland_test_preview.html"
    },
    {
      "source": "/preview",
      "destination": "/holland_test_preview.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=3600, s-maxage=3600"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        }
      ]
    }
  ]
}
```

### 关键文件

确保以下文件存在且已提交：
- ✅ `vercel.json` - Vercel 配置
- ✅ `holland_test_preview.html` - 测试页面（120题）
- ✅ `index.html` - 入口页面

## 🔍 验证部署

部署完成后，测试以下 URL：

1. **根路径**
   ```
   https://holland-test.snowshadow.com.cn/
   ```
   应该显示测试页面

2. **预览路径**
   ```
   https://holland-test.snowshadow.com.cn/preview
   ```
   应该显示测试页面

3. **直接文件访问**
   ```
   https://holland-test.snowshadow.com.cn/holland_test_preview.html
   ```
   应该能直接访问文件

## ⚠️ 如果部署失败

### 检查部署日志

1. 在 Vercel 控制台 → Deployments
2. 点击失败的部署
3. 查看 "Build Logs" 或 "Function Logs"
4. 查找错误信息

### 常见问题

**问题 1：部署显示 "Build Failed"**
- 检查 vercel.json 语法是否正确
- 查看构建日志中的错误信息

**问题 2：部署成功但页面404**
- 检查 vercel.json 的 rewrites 配置
- 确认文件路径正确

**问题 3：页面显示旧版本**
- 清除浏览器缓存（Ctrl+Shift+R）
- 检查 Vercel 部署日志确认最新版本已部署

## 📊 部署状态监控

### Vercel 控制台位置

- **项目列表**：https://vercel.com/dashboard
- **部署历史**：项目 → Deployments
- **项目设置**：项目 → Settings
- **域名配置**：项目 → Settings → Domains

### 部署状态说明

- **Ready** ✅：部署成功，可以访问
- **Building** ⏳：正在构建，请等待
- **Error** ❌：部署失败，查看日志
- **Queued** ⏸️：排队中，等待构建

## 🎯 下一步

1. ✅ 确认所有更改已推送到 GitHub
2. ✅ 检查 Vercel 控制台的部署状态
3. ✅ 等待部署完成（1-2分钟）
4. ✅ 测试部署的 URL
5. ✅ 验证功能是否正常

---

**最后更新**：所有更改已推送到 GitHub，Vercel 应该会自动部署。

